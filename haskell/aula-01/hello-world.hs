areaQuadrado b a = b * a

soma x y = x + y

soma3 x y z = soma (soma x y) z

media3 x y z = (soma3 x y z) / 3

media4 x y z w = (soma (soma3 x y z) w) / 4

multiplica x y = x * y

divide x y = x / y

hipotenusa a b = sqrt (soma (multiplica a a) (multiplica b b))